In order to add a headshot to a textbox:
  1.  Set the base sprite in sprite renderer.
  2.  Set the animator to use in animator.
  3.  Make sure the render mode is on "Screen Space - Camera"
    3a. Set the render camera to Main Camera.
